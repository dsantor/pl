// Generated by CoffeeScript 1.12.7
(function() {
  this.UserDetailsDialog = (function() {
    function UserDetailsDialog() {
      this.container = $('.js--side--dialog--container');
      UserService.loadProfileTemplate(null, this, this._loadProfileTemplateSucess, null);
      this.userId = null;
      this.user = null;
    }

    UserDetailsDialog.prototype.show = function(userId) {
      this.userId = userId;
      this.container.attr('visibility', true);
      return UserService.getUser(this.userId, null, this, this._getUserSuccess, null);
    };

    UserDetailsDialog.prototype.destroy = function() {
      this.container.html('');
      this.container = null;
      this.userId = null;
      return this.user = null;
    };

    UserDetailsDialog.prototype._loadProfileTemplateSucess = function(template) {
      this.container.html(template);
      this.firstname = this.container.find(".js--firstname");
      this.lastname = this.container.find(".js--lastname");
      this.phone = this.container.find(".js--phone");
      this.email = this.container.find(".js--email");
      this.street = this.container.find(".js--street");
      this.city = this.container.find(".js--city");
      return this.buildNumber = this.container.find(".js--buildNumber");
    };

    UserDetailsDialog.prototype._getUserSuccess = function(user) {
      this.firstname.text(user.firstName);
      this.lastname.text(user.lastName);
      this.phone.text(user.phoneNumber);
      this.email.text(user.email);
      this.street.text(user.street);
      this.city.text(user.city);
      return this.buildNumber.text(user.buildNumber);
    };

    return UserDetailsDialog;

  })();

}).call(this);
